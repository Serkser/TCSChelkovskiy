<UserControl x:Class="TCSChelkovskiy.Controls.VacancyBlock"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TCSChelkovskiy.Controls" 
             mc:Ignorable="d" d:DesignWidth="500" >
    <Grid>
        <StackPanel Margin="10,10,0,0" DataContext="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=UserControl}}">
            <TextBlock HorizontalAlignment="Left" 
                       Text="{Binding Title}"
                       TextWrapping="Wrap" 
                       VerticalAlignment="Top"/>

            <TextBlock HorizontalAlignment="Center" 
                       Margin="0,20,0,0" 
                       Text="{Binding Description}"
                       TextWrapping="Wrap" 
                       MaxWidth="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=UserControl}, Path=ActualWidth}"
                       VerticalAlignment="Top"/>
        </StackPanel>
    </Grid>
</UserControl>
